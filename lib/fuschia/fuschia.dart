import 'package:flutter/material.dart';

class Fuschia extends StatelessWidget {
  const Fuschia({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
